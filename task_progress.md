# 起名引擎改进任务

## 分析和已发现的问题
- [x] 分析拼音错误根源：`COMMON_PINYIN_MAP` 硬编码映射表仅覆盖约200汉字
- [x] 分析名字过于集中的原因：AI prompt缺乏分散约束 + 后处理无去重逻辑

## 实施步骤
- [ ] 安装 `pinyin-pro` 拼音库
- [ ] 用 `pinyin-pro` 替换 `COMMON_PINYIN_MAP` 硬编码映射 + `validateAndFixPinyin` 函数
- [ ] 增强AI prompt：要求名字用字更分散，避免重复使用同一字
- [ ] 添加后处理去重/分散逻辑：限制同一字在不同名字中出现的次数
- [ ] 测试验证：生成结果是否更分散、拼音是否正确