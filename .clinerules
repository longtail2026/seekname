## 核心语言指令
你必须始终使用与用户相同的语言进行交流。
当用户使用中文时，你的全部思考过程、推理逻辑以及最终回复，都必须使用中文。

## Token 节省规则

### 输出精简
1. 回答直接给出代码/方案，不输出无关解释和问候语
2. 不重复描述显而易见的代码逻辑
3. 代码注释只写必要部分（复杂逻辑或业务关键点）
4. 修改文件时用 replace_in_file 而非 write_to_file，只发送变更部分
5. 仅读取与当前任务直接相关的文件，避免读取无关大文件
6. 不使用"好的"、"明白了"、"首先"、"然后"等冗余过渡词

### 上下文管理
1. 在不丢失任务上下文的前提下，及时清理已完成任务
2. 每次交互尽量高效，一个消息内完成一个完整步骤